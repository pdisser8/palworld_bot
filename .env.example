# Discord bot token from the Developer Portal
DISCORD_TOKEN=

# Channel the server's console output gets streamed to
LOG_CHANNEL_ID=

# Channel where the !restart command is accepted (can be the same as LOG_CHANNEL_ID)
CONTROL_CHANNEL_ID=

# Comma-separated list of Discord user IDs allowed to trigger a restart
ALLOWED_USER_IDS=

# Command text (not a slash command) that triggers a restart, matched case-insensitively
RESTART_COMMAND=!restart

# Seconds of warning given to players before the machine actually reboots
RESTART_DELAY_SECONDS=30

# Full path to PalServer.exe (or the .bat that launches it)
SERVER_EXE_PATH=C:\PalServer\PalServer.exe

# Optional space-separated args passed to the server exe
SERVER_ARGS=

# Working directory to launch the server from (defaults to the exe's folder)
SERVER_CWD=

# Local file that receives a copy of the server console output
SERVER_LOG_PATH=palworld_server.log

# Case-insensitive regex used to print possible player connection events
PLAYER_EVENT_PATTERN=login|logged in|joined|connected
